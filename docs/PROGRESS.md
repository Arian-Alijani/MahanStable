# PROGRESS — Living state (هر سشن آپدیت کن)

> شروع هر سشن: اول `docs/AI_CONTEXT.md` (نقشه) بعد این رو. آخر هر تسک: آپدیت کن. این فایل = حافظه بین سشن‌ها.

## Current phase
**Phase 9 — Admin Panel 🔄 در حال توسعه (پایه پیاده‌سازی‌شده و زنده).**
P0–P8 ✅ تمام. پنل ادمین (`Areas/Admin`، ۳۲ صفحهٔ Razor، `Features/Admin` ۶۱ فایل) شامل: داشبورد، محصولات (+wizard/MultiBrand)، تنوع/موجودی (+CSV)، دسته/برند/ویژگی/تگ، سفارش، کاربر، مدیریت صفحهٔ اصلی.
قدم بعد = تکمیل ادمین سپس **P10 Security · P11 Perf · P12 Deploy**.

### ⚠️ Dev setup لازم (قبل اجرا)
- `Otp__Pepper` باید ست شود (env یا `appsettings.Development.json`) وگرنه `OtpHasher` در اولین لاگین throw می‌کند. هیچ default در سورس.
- بدون `Sms__ApiKey` در dev → `FakeSmsSender` (کد در لاگ سرور warn). با کلید → SMS.ir واقعی (نیاز `Sms__TemplateId`).
- ادمین اولیه (`DataSeeder`) = `09037882674` — لاگین OTP با این شماره → دسترسی `/Admin`. نقش Admin هر درخواست از DB re-check می‌شود (`OnValidatePrincipal`).

## Prerequisites (نصب‌شده)
> ⚠️ ردیف‌های زیر = **محیط dev ویندوزیِ کاربر**. در **sandbox لینوکسیِ AI هیچ‌کدام پیش‌فرض نیست**. برای sandbox: `source tools/setup-dotnet.sh` (dotnet ~17s) — کامل: **docs/ENV_TESTING.md**.
- .NET 8 SDK = 8.0.422 ✅ (مسیر: `C:\Program Files\dotnet`؛ تو bash جدا `export PATH="/c/Program Files/dotnet:$PATH"`)
- dotnet-ef tool = 10.0.9 ✅ (با net8 سازگار)
- git 2.53 ✅
- SQL Server 2019 LocalDB ✅ — instance `MSSQLLocalDB` (v15.0) Running. SqlLocalDB.exe: `C:\Program Files\Microsoft SQL Server\150\Tools\Binn\`. Dev conn در `appsettings.Development.json` (gitignored).
- مدل داده: **بازطراحی تمیز** (داده قدیم مهم نیست).

## Decisions locked
- .NET 8 LTS / net8.0 ✅
- SQL Server 2019 ✅
- Clean Arch + MediatR CQRS ✅
- Razor server-render + Bootstrap 5 + jQuery (بدون Node/SPA) ✅
- Plesk IIS in-process ✅
- UI مشابه `Old UI` با ارتقا (جزییات بعداً) ✅
- **Domestic-only**: سایت بدون اینترنت بین‌الملل کامل کار کنه — صفر CDN/سرویس خارجی، همه asset self-host، سرویس فقط داخلی ✅ (جزییات: CLAUDE.md + CONVENTIONS.md)

## Next steps
پنل ادمین (Phase 9) پایه پیاده‌سازی شده و زنده است — در حال تکمیل/پالایش. baseline تمیز قبل از پنل ادمین = tag `baseline-before-admin-panel` (دستور بازگشت کامل: `git reset --hard baseline-before-admin-panel`). مرز کد ادمین = `Areas/Admin`, `wwwroot/admin`, `wwwroot/AdminPanel`, `Features/Admin` + بلوک‌های مارکردار `=== ADMIN-PANEL START/END ===` در `Program.cs`. فازها:
- [x] P0 Scaffold · [x] P1 Domain+Data · [x] P2 Infra+Config · [x] P3 Layout+Home
- [x] P4 Catalog (4A✅ / 4B✅ / 4C❌حذف) · [x] P5 Auth · [x] P6 Cart+Checkout · [x] P7 Payment
- [x] P8 UserPanel · [~] P9 Admin (پایه زنده، در حال تکمیل) · [ ] P10 Security · [ ] P11 Perf · [ ] P12 Deploy

## Open questions (از کاربر بپرس وقتی رسیدی)
- جزییات ارتقای UI نسبت به نمونه؟
- SMS provider دقیق کدوم؟ (نمونه: کلید generic داشت)
- مدل داده نهایی — همون نمونه یا تغییر؟

## Changelog
- 2026-06-11: استک قفل شد، docs ساخته شد.
- 2026-06-11: پیش‌نیاز نصب (.NET8 SDK + dotnet-ef). **P0 Scaffold تمام**: sln + 5 پروژه net8.0، refها، پکیج‌ها (MediatR12/EFCore8/FluentValidation11/QuestPDF)، web.config in-process، .gitignore، appsettings.sample. build سبز، Home HTTP 200.
- 2026-06-11: **P1 Domain+Data تمام**: 17 entity + BaseEntity + 2 enum (بازطراحی تمیز)، IApplicationDbContext، MyDbContext، 4 فایل Fluent config (DeleteBehavior دقیق برای جلوگیری از multiple-cascade-path)، DependencyContainer، Program.cs بازنویسی. migration `InitialCreate` ساخته و رو LocalDB اعمال شد → 18 جدول (17 + __EFMigrationsHistory). build سبز.
- 2026-06-11: **P2 Infra+Config تمام**: `Application.DependencyInjection.AddApplicationLayer` (MediatR12 RegisterServicesFromAssembly + FluentValidation AddValidatorsFromAssembly). `SmsSettings`/`ZarinpalSettings` در `Application/Common/Settings` → bind از env (`Sms__ApiKey`, `Zarinpal__MerchantId`, `Zarinpal__CallbackUrl`) via `Configure<T>` در IoC (پکیج `Microsoft.Extensions.Options.ConfigurationExtensions` اضافه شد). Program.cs: anti-forgery گلوبال (`AutoValidateAntiforgeryTokenAttribute`) + `AddHsts` (365d, IncludeSubDomains). HTTPS redirect موجود. build سبز، app بالا اومد، DI بدون خطا resolve، Home HTTP 200.
- 2026-06-11: **P3 Layout+Home تمام (data-driven، admin-controllable)**:
  - **Domain**: entity `Banner` + `HomeSection`، enum `HomeSectionType`(ProductRow/PromoBanner) + `HomeProductSource`(Featured/BestSelling/Newest/Discounted/ByCategory)، `Category.ShowOnHome`. Fluent config `HomeConfigurations` (FK Category `OnDelete SetNull`، index روی DisplayOrder). migration `Add_HomeContent` رو LocalDB اعمال شد.
  - **Application (CQRS)**: `GetHomePageQuery` (AsNoTracking؛ banners + featured categories + sectionها به‌ترتیب DisplayOrder با `.Include(Category)`؛ Discounted = `DiscountPrice!=null && <Price`؛ محاسبه DiscountPercent؛ rowهای خالی skip). `GetMenuCategoriesQuery` (درخت in-memory از ShowInMenu). DTOها: `ProductCardDto`/`HomePageViewModel`/`HomeSectionViewModel`/`MenuCategoryDto` — entity مستقیم به View نمی‌ره.
  - **Seed**: `DataSeeder` (Development-only، idempotent): 3 brand، 6 category همه ShowOnHome، 4 رنگ، 12 محصول (۹ تخفیف‌دار)، 3 banner، 7 HomeSection (Featured→Discounted→PromoBanner full→Newest→ByCategory(case)→2× half promo).
  - **Web/UI**: RTL `_Layout` (`dir=rtl lang=fa`، bootstrap.rtl.min.css + site.css، meta/canonical/OG داینامیک، skip-link)، `_Header` (لوگو SVG، search، cart popover، CTA ورود، mega-menu، drawer موبایل — **بدون محبوب‌ها/favorite**)، `_Footer` دارک، `CategoryMenu` ViewComponent، partialهای Home (`_HeroBanner`/`_CategoryGrid`/`_ProductRow`/`_PromoBanner`/`_ProductCard`). فونت Vazir self-host، آیکن‌ها inline SVG، placeholderها SVG محلی. site.css (design system کامل) + site.js (drawer/cart/hero-slider/drag-carousel/add-cart).
  - **Verify**: build سبز (0/0)، Home HTTP 200، 4 ردیف محصول، ردیف تخفیف فقط 9 کارت/9 بج (صرفاً تخفیف‌دار)، audit domestic-only پاک (صفر CDN/دامنه خارجی)، صفر favorite.
- 2026-06-11: **P4 به‌خواست کاربر به ۳ بخش تقسیم شد (4A/4B/4C). 4A — Catalog list تمام (ظاهر «مشابه بخش کتگوری Plus Version»):**
  - **Application (CQRS)** `Features/Catalog/Queries/GetCatalog/`: `CatalogFilter` (CategorySlug/BrandSlug/ColorId/Search/PriceMin/PriceMax/InStockOnly/Sort/Page/PageSize)، `GetCatalogQuery`، `CatalogViewModel` + facet DTOها (`CategoryFilterDto`/`ColorFilterDto`/`BrandFilterDto`)، `GetCatalogQueryHandler` (AsNoTracking؛ دسته+زیرشاخه‌ها via پیمایش درخت در حافظه؛ Search روی Title/ShortDescription/Brand.Name؛ قیمت روی `DiscountPrice ?? Price`؛ ۵ سورت newest/oldest/price-asc/price-desc/popular؛ Count سپس Skip/Take؛ facetها = برند/رنگ/قیمت‌floor‌ceil). **reuse** `ProductCardDto` + partial `_ProductCard`.
  - **Web**: `CatalogController` (Index/Category/Search). Routes تمیز SEO در Program.cs: `/products`, `/category/{slug}`, `/search` (قبل از default). Viewها: `Catalog/Index` (فرم GET سراسری + گرید + no-results)، `_FilterSidebar` (accordion دسته لینک‌محور + radio برند/رنگ + dual price-range + toggle موجودی + reset)، `_ProductsHeader` (select سورت داخل فرم + شمارش + chipهای فیلتر فعال لینک‌حذف + دکمه فیلتر موبایل)، `_Pagination` (لینک‌محور با حفظ کامل query، window ±2 + …). همه فیلتر **سمت سرور** (بدون SPA)؛ بدون JS هم با دکمه «اعمال» کار می‌کند.
  - **CSS** (`site.css`): بلوک Catalog با همان tokenها (پالت = Plus Version) — `.catalog-shell/.catalog-grid` (۲→۴ ستون)، `.filter-sidebar`+drawer موبایل+overlay، `.filter-accordion`، `.category-btn`(+is-active)، radio/swatch، dual `.price-range`+fill، `.toggle`، `.products-header`+`.sort-select`، `.filter-chip`، `.no-results`، `.pagination`. آیکن‌ها inline SVG.
  - **JS** (`site.js`): ماژول catalog — drawer موبایل (open/close/overlay/Esc)، auto-submit روی دسکتاپ on change (radio/checkbox/select)، dual price slider (sync label+fill، debounce submit، hidden فقط وقتی از floor/ceil جدا شد).
  - **Verify**: build سبز (0/0). `/products` 200 + ۱۲ کارت seed. `/category/mobile` 200 → ۴ کارت (زیرمجموعه). `/search?q=قاب` → ۲. سورت price-asc/desc واقعاً برعکس. brand=samsung → ۶. PriceMax=1 → ۰ + no-results. `?PageSize=4` → pagination، Page=2 → ۴ کارت. audit domestic-only پاک (صفر CDN/دامنه خارجی روی Views/css/js جدید)، صفر favorite. (نکته: لینک کارت `/product/{slug}` تا 4B عمداً 404 می‌دهد.)
- 2026-06-11: **4B — صفحه جزییات محصول تمام (ظاهر «مشابه product-detail پلاس‌ورژن»؛ به‌خواست کاربر فقط پیاده‌سازی، بدون runtime smoke):**
  - **Application (CQRS)** `Features/Catalog/Queries/GetProductDetail/`: `ProductDetailDto` (+ `ProductImageDto`/`ProductColorDto`/`ProductFeatureDto`/`ProductTagDto`؛ computed `FinalPrice`/`HasDiscount`)، `GetProductDetailQuery(Slug)`، `GetProductDetailQueryHandler` (AsNoTracking؛ `IsActive && Slug==`؛ Images مرتب IsMain سپس DisplayOrder، Features مرتب Feature.DisplayOrder، Colors/Tags؛ `FirstOrDefaultAsync`؛ null اگر نبود).
  - **Web**: `CatalogController.Detail(slug)` → NotFound اگر null وگرنه View. Route تمیز `product/{slug}` (بعد از category). View `Catalog/Detail` = گرید ۳ ستون (گالری/info/buy) + تب‌ها full-width: breadcrumb، `pd-media` با main image + nav prev/next + thumbها، رنگ‌ها (radio)، bulletها، استوک، buy box sticky + CTA `data-add`، تب‌های معرفی/مشخصات. JSON-LD Product. آیکن inline SVG، **بدون favorite/CDN**.
  - **CSS** `wwwroot/css/product-detail.css` (لود via `@@section Styles`): کلاس‌های `pd-*` روی همان tokenها — `pd-grid` (۱→۳ ستون)، `pd-media`/`pd-main-image`(fade)/`pd-nav-btn`/`pd-thumb`(is-active)، `pd-color`(`:has(:checked)`)، `pd-buy`(sticky)/`pd-cta`، `pd-tabs`/`pd-tab`/`pd-panel`([hidden])/`pd-specs`.
  - **JS** (`site.js`): ماژول product-detail — سواپ تصویر اصلی با کلیک thumb (fade + fallback setTimeout)، nav prev/next چرخشی، سویچ تب (is-active + aria-selected + hidden). `data-add` از قبل به بازخورد سبد وصل بود.
  - **Verify**: build سبز (0/0). runtime smoke طبق خواست کاربر اجرا نشد.
- 2026-06-11: **P5 Auth تمام (ورود/ثبت‌نام OTP پیامکی دو مرحله‌ای + امنیت کامل):**
  - **Domain/Data**: `OtpCode.Code` → `CodeHash`(nvarchar 128) + `Attempts`. migration `Add_Otp_Hash` رو LocalDB اعمال شد.
  - **Application (CQRS)** `Features/Auth/Commands/`: `SendLoginCode` (normalize شماره، rate-limit، تولید کد CSPRNG، هش HMAC، ذخیره، ارسال) + `VerifyLoginCode` (مقایسه constant-time، انقضا/تک‌مصرف، قفل brute-force، find-or-create کاربر، بازگشت claim). Validatorها (FluentValidation). `PhoneNumberHelper` (نرمال‌سازی موبایل ایران؛ ارقام فارسی/عربی، +98/0098/98). `OtpSettings`/`SmsSettings`(TemplateId). `ValidationBehavior` MediatR pipeline + ثبت در DI.
  - **Infra** `Services/`: `OtpHasher` (HMAC-SHA256 + Pepper از env، `FixedTimeEquals`)، `SmsIrSender` (SMS.ir verify/send، typed HttpClient، کلید از env)، `FakeSmsSender` (dev، کد در لاگ). IoC: انتخاب Fake/SMS.ir بر اساس وجود ApiKey + `Configure<OtpSettings>` + `Microsoft.Extensions.Http`.
  - **Web**: `AccountController` (Login GET/POST → Verify GET/POST → SignIn cookie؛ Logout POST-only؛ open-redirect-safe با `Url.IsLocalUrl`). viewها `Account/Login`+`Verify` (asp tag-helper → anti-forgery خودکار، `auth.css` self-host). `_Header` حالت لاگین (نام + خروج). Program.cs از قبل cookie auth + session داشت.
  - **Security review** (skill + sub-agents): open-redirect/CSRF/privilege-escalation/XSS/crypto همه پاک. ۲ یافته رفع شد: **(۱) enumeration oracle** — پاسخ Send یکسان شد (همیشه success، throttle بی‌صدا، بدون نشت cooldown/seconds). **(۲) brute-force** — کد ۵→۶ رقم + قفل سطح-شماره `MaxVerifyAttemptsPerHour` (مستقل از resend) علاوه بر قفل ردیف `MaxAttempts`.
  - **Verify**: build سبز (0/0). runtime smoke: Login GET 200، Send 302→Verify + کد ۶رقمی در FakeSms، resend بی‌صدا یکسان (oracle رفع)، Verify درست → 302 خانه + کوکی auth، UI لاگین/خروج، کد غلط/شماره نامعتبر/cooldown پیام عمومی encode-شده، Logout POST 302 + کوکی پاک، Logout GET 405.
- 2026-06-11: **4C (کامنت/امتیاز) ❌ به‌خواست کاربر حذف شد** («نیاز به امتیازدهی و بخش کامنت نداریم، این بخش لازم نیست»). کدی نوشته نشد. `ProductComment` entity در Domain می‌ماند ولی بلااستفاده. ⇒ **Phase 4 (Catalog) کامل تمام**. قدم بعد = Phase 5 (Auth).
- 2026-06-11: **P6 Cart + Checkout تمام (سبد session-based + ثبت سفارش Pending؛ build سبز + runtime smoke پاس):**
  - **امنیت محوری**: session فقط `ProductId/ColorId/Quantity` نگه می‌دارد — **هیچ قیمتی** client-side. قیمت همیشه سمت سرور از DB محاسبه (`GetCartQuery` نمایش، `PlaceOrderCommandHandler` ثبت) → ضد دستکاری قیمت. anti-forgery گلوبال روی همه POST تأیید شد (add بدون توکن → 400). `[Authorize]` روی Checkout (unauth → 302 login). `UserId` همیشه از claim نه فرم. IDOR guard: آدرس باید متعلق به UserId باشد. `OrderCode` غیرقابل‌حدس `MS-{yyMMddHHmmss}-{rnd:D6}` via RandomNumberGenerator.
  - **Application (CQRS)** `Features/Cart/`: `CartItemInput`، `GetCartQuery`+Handler (enrich از DB، `finalPrice=(DiscountPrice??Price)+color.PriceModifier`، clamp qty به stock، drop inactive/zero-stock، `HasUnavailable`)، `PlaceOrderCommand`+Handler+Validator (چک stock/price از DB، Order(Pending)+OrderItems با snapshot قیمت، `OrderItem.ColorId=pc?.ColorId`، **stock کم نمی‌شود — P7 بعد پرداخت**). `Features/Account/`: `GetUserAddressesQuery`+Handler (فیلتر UserId)، `AddAddressCommand`+Validator (PostalCode `^\d{10}$`، phone normalized)+Handler. `ProductDetailDto.ProductColorDto` فیلد `Id` اضافه.
  - **Web**: `CartStore` (session key "cart"، JSON، MaxQtyPerLine=20/MaxLines=50، merge same product+color). `CartController` (Index + Add/Update/Remove ajax → Json{count,payable}). `CheckoutController` `[Authorize]` (Index/AddAddress/Place/Placed). Program.cs: `AddHttpContextAccessor`+`AddScoped<CartStore>`+routeهای `/cart` و `/checkout`. `_Layout` توکن `@Html.AntiForgeryToken()` گلوبال. `_Header` دکمه سبد → لینک `/cart`. `Detail` رادیو رنگ `value=@c.Id`. site.js: add-to-cart واقعی ajax (توکن از header `RequestVerificationToken`، colorId از رادیو انتخاب‌شده). cart.js: inc/dec/remove ajax + به‌روزرسانی badge/خلاصه.
  - **Views/assets**: `Cart/Index`، `Checkout/Index` (انتخاب/افزودن آدرس + خلاصه + ثبت)، `Checkout/Placed`، `wwwroot/css/cart.css` (با همان design tokens). audit domestic-only پاک.
  - **Verify (runtime smoke)**: build سبز (0/0). add productId=1 qty=2 → `{count:2,payable:115800000}`. cart نمایش line + sum-qty=2. update qty=1 → payable نصف (سرور recompute). remove → count=0. `/checkout` unauth → 302 `/Account/Login?ReturnUrl=%2Fcheckout`. add بدون CSRF token → 400. (ثبت کامل سفارش نیازمند auth+address — کد + IDOR guard آماده، auth gate تأیید شد.)
- 2026-06-11: **P7 — Payment (Zarinpal) تمام (پیاده‌سازی + runtime smoke):**
  - **Application**: `IPaymentGateway` (RequestAsync/VerifyAsync با امانت toman؛ recordهای `PaymentRequestResult`/`PaymentVerifyResult`). `ZarinpalSettings` + Sandbox toggle و endpointهای محاسبه‌شده (Host/RequestUrl/VerifyUrl/StartPayUrl). CQRS `Features/Payment/`: `StartPaymentCommand` (load سفارش با Id+UserId = IDOR guard؛ guard وضعیت Paid/غیر Pending-AwaitingPayment/FinalAmount<=0؛ gateway.RequestAsync؛ ایجاد `Payment{Pending}` + Order→AwaitingPayment؛ خروجی RedirectUrl)، `VerifyPaymentCommand` (یافتن Payment با Authority؛ **idempotent** اگر قبلاً Success؛ Status!=OK → Failed + Order→Pending؛ وگرنه gateway.VerifyAsync؛ موفق → Payment.Success+RefId، Order.Paid+PaidAt+TrackingCode، `DecrementStockAsync` فقط بعد پرداخت موفق — کسر استوک ProductColor در صورت ColorId وگرنه Product، clamp `Math.Max(0,…)`؛ code 100 موفق/101 already-verified).
  - **Infra.Data**: `ZarinpalGateway` (typed HttpClient، POST JSON با `[JsonPropertyName]`، `ToRial(toman)=toman*10`، try/catch → خطای generic). DI: `AddHttpClient<IPaymentGateway,ZarinpalGateway>` timeout 20s.
  - **Web**: `PaymentController` `[Authorize]` — `[HttpPost] Start(orderId)` → StartPaymentCommand؛ موفق `Redirect(RedirectUrl)` (external به درگاه، نه LocalRedirect)؛ خطا → TempData["PaymentError"]+بازگشت Placed. `[AllowAnonymous][HttpGet] Verify(Authority,Status)` → VerifyPaymentCommand → ViewBag → View. routeهای `payment/start` + `payment/verify` در Program.cs. `Checkout/Placed` دکمه «پرداخت آنلاین» (POST orderId) + لینک «بعداً». View جدید `Payment/Verify` (حالت موفق RefId/کد سفارش یا ناموفق + پیام خطا، با `cart.css`).
  - **Verify (runtime smoke)**: build سبز (0/0). POST `/payment/start` unauth → 302 `/Account/Login?ReturnUrl=%2Fpayment%2Fstart`. GET `/payment/verify?Status=NOK` → 200 و رندر «پرداخت ناموفق». audit domestic-only پاک (درگاه = دامنه داخلی Zarinpal، صفر CDN).
- 2026-06-12: **P7 درگاه ست شد:** بخش `Zarinpal` در `appsettings.Development.json` (gitignored) — `MerchantId` از پروژه قدیم `MyEshop_Phone-master` (`253fa665-…`)، `Sandbox=true` (تست dev)، `CallbackUrl=https://localhost:7076/payment/verify`. binding via موجود `Configure<ZarinpalSettings>(GetSection("Zarinpal"))` بدون تغییر کد. build سبز (0/0)، app boot پاک، `/payment/verify?Status=NOK` 200، Home 200، صفر exception. (prod: همین کلید در Plesk env + `Sandbox=false` + callback دامنه واقعی — P12.)
- 2026-06-12: **P7 security-review (caveman + خواندن کامل flow) — ۳ یافته رفع شد:**
  - 🔴 **double stock-decrement (verify همزمان/replay):** idempotency فقط بعد commit کار می‌کرد؛ دو `GET /payment/verify` همزمان هر دو Pending می‌دیدند → کسر مضاعف. رفع: `Order.RowVersion` (rowversion، migration `Add_Order_RowVersion` رو LocalDB اعمال شد) + catch `DbUpdateConcurrencyException` در Verify → تراکنش بازنده commit رد می‌شود، موجودی دوباره کم نمی‌شود.
  - 🔴 **multi-Authority finalize:** `Start` چندبار → چند `Payment{Pending}` با Authority متفاوت؛ verify دومی بعد Paid شدن سفارش، order را دوباره Paid + استوک دوباره کم می‌کرد. رفع: guard `if order.Status==Paid → Payment=Failed، بدون کسر، بازگشت موفق با TrackingCode موجود`.
  - 🟡 **code 101 re-decrement:** مسیر AlreadyVerified هم از همان guardهای بالا عبور می‌کند → کسر دوباره ندارد.
  - تایید پاک: مبلغ سمت سرور از DB (ضد دستکاری)، Authority توکن غیرقابل‌حدس، `[AllowAnonymous]` روی callback درست (درگاه کوکی ندارد)، IDOR guard در Start (Id+UserId). build سبز (0/0)، migration اعمال، boot پاک، verify 200/home 200.
- 2026-06-12: **P8 — User Panel تمام (ظاهر «مشابه Plus Version»؛ Razor Pages زیر `/Panel`):**
  - **Application (CQRS)** `Features/Account/`: Queries `GetUserProfile` (UserProfileDto: نام/شماره/ایمیل/تعداد سفارش/عضویت‌از)، `GetUserOrders` (UserOrderDto لیست — مخفی‌کردن Pending رهاشده، thumb از Product.Images مرتب IsMain→DisplayOrder)، `GetOrderDetail` (OrderDetailDto + **IDOR guard** `Id==OrderId && UserId==UserId`، آدرس/آیتم‌ها/`LineTotal`). Commands `UpdateProfile` (نام/ایمیل + Validator)، `ChangePhone/SendChangePhoneCode` (یکتایی شماره، rate-limit/cooldown، کد CSPRNG هش‌شده، SMS) + `VerifyChangePhone` (قفل brute-force، مقایسه constant-time، اعمال PhoneNumber جدید).
  - **Web (Razor Pages)** `Pages/Panel/` با `AuthorizeFolder("/Panel")` در Program.cs: `_ViewStart`/`_ViewImports`، `PanelPageModel` (base؛ `UserId` از claim NameIdentifier)، `_PanelSidebar` (آواتار+nav+logout). صفحات: `Index` (داشبورد: hero + آمار + کارت سفارش‌ها)، `Profile` (ویرایش نام/ایمیل + تغییر شماره دومرحله‌ای OTP؛ موفق → SignOut + ری‌لاگین چون claim شماره کهنه می‌شود)، `OrderDetail` (جدول + خلاصه + لینک فاکتور)، `Invoice` (PDF). `Helpers/OrderStatusView` (Label/Tone فارسی). `Services/InvoicePdfService` (QuestPDF، فونت Vazir self-host، A4 RTL — هدر/مشتری/آدرس/جدول/خلاصه).
  - **CSS** `wwwroot/css/panel.css` (همان design tokens، پالت #2563EB): layout sidebar+content، `panel-card/panel-user/panel-avatar/panel-nav`، `order-card`+thumbها، `status-badge` (tones is-success/warn/info/danger/muted)، `info-grid/info-card/panel-input`(+otp)، `od-table/od-summary`، `addr-card`، responsive (≤860 single-col). آیکن inline SVG، domestic-only.
  - **Verify**: build solution سبز (0/0). header از قبل `/Panel` لینک می‌داد. (نکته: PDF فونت Vazir از `wwwroot/fonts/vazirmatn/Vazir.ttf`.)
  - **Runtime smoke (پاس):** login OTP → `/Panel` 200 (داشبورد + آمار + کارت سفارش)، `/Panel/Profile` 200، `/Panel/OrderDetail?id=` 200 (جدول + خلاصه)، `/Panel/Invoice?id=` → PDF معتبر (`%PDF-`, فونت `/AAAAAA+Vazir` embed، Identity-H/FontFile2 → فارسی درست). IDOR: anon→302 login، order ناموجود→404. anti-forgery گلوبال فعال.
  - 🐛 **رفع‌شده حین smoke:** `InvoicePdfService._fontName` بود `"Vazirmatn"` ولی family واقعی TTF = `Vazir` (تایید via GDI PrivateFontCollection) → PDF به SegoeUI/Lato fallback می‌کرد (فارسی tofu). اصلاح به `"Vazir"`؛ PDF بازتولید → Vazir embed شد (size 96KB→144KB glyphها). build سبز (0/0).
- 2026-06-12: **Phase 9 (Admin Panel) — برنامه‌ریزی قابل‌برگشت (هیچ کدی نوشته نشد):** تلاش قبلی پنل ادمین نیمه‌کاره رها شده بود. وضعیت سورس بررسی شد ⇒ در عمل **هیچ کد پنل ادمینی وجود ندارد** (نه `Areas/Admin`، نه `Pages/Admin`، نه `Features/Admin`، نه Controller/CSS/JS ادمین؛ فقط ارجاع‌های عمومی `User.IsAdmin` و claim نقش Auth). یعنی پروژه همین حالا در حالت تمیزِ «قبل از پنل ادمین» است. اقدامات:
  - **tag `baseline-before-admin-panel`** روی commit `f279aa6` (مقصد بازگشت کامل: `git reset --hard baseline-before-admin-panel`).
  - **`docs/ADMIN_ROADMAP.md`** — رودمپ ۴ مرحله‌ای پنل ادمین (۱:Shell+AuthGate، ۲:کاتالوگ CRUD+آپلود، ۳:Variant/موجودی+ویژگی/تگ، ۴:سفارش/کاربر/داشبورد)، با مرزبندی فیزیکی کد ادمین (`Areas/Admin`، `wwwroot/admin`، `wwwroot/AdminPanel`، `Features/Admin`)، تنها فایل مشترکِ قابل‌تغییر = `Program.cs` فقط داخل بلوک نشان‌دار، migration افزایشی `Add_Admin_*`، و «Boundary list» + دستور rollback برای هر مرحله.
  - **`docs/ADMIN_PROGRESS.md`** — فایل پیشرفت زندهٔ مخصوص پنل ادمین (حافظهٔ بین‌سشنی): وضعیت فعلی=مرحله ۰، چک‌لیست ۴ مرحله، «دفتر ثبت rollback» (commit/migration هر مرحله)، و سؤالات باز.
  - **`CLAUDE.md`** آپدیت: شروع سشنِ کار ادمین → خواندن `ADMIN_PROGRESS.md`؛ افزودن دو فایل به Pointers.
- 2026-06-13: **اصلاحات صفحهٔ اصلی + هدر + بخش جدید پنل (به‌خواست کاربر):**
  1. **حذف ردیف‌های محصول از صفحهٔ اصلی:** در `Views/Home/Index.cshtml` حلقهٔ رندر سکشن‌ها فقط `PromoBanner` را رندر می‌کند؛ هر سکشن `ProductRow` با `continue` نادیده گرفته می‌شود. (partial `_ProductRow.cshtml` روی دیسک می‌ماند ولی دیگر فراخوانی نمی‌شود — قابل‌فعال‌سازی مجدد در آینده.)
  2. **لینک دسته‌ها در هدر → تمام محصولات با فیلتر همان دسته:** `Components/CategoryMenu/Default.cshtml` (دسته‌های اصلی + زیرشاخه‌ها) و `Home/_CategoryGrid.cshtml` از `/@c.Slug` (که route نداشت) به `/products?CategorySlug=@Uri.EscapeDataString(slug)` تغییر کرد → کاربر به `CatalogController.Index` با فیلتر دسته هدایت می‌شود.
  3. **بنر بالای سایت:** دکمه‌های جابجایی (فلش prev/next + نقطه‌ها) از `Home/_HeroBanner.cshtml` حذف شد؛ در `wwwroot/js/site.js` هندلرهای dot/arrow حذف و بازهٔ auto-advance از 5000ms به **15000ms (۱۵ ثانیه)** تغییر کرد (توقف on-hover حفظ شد).
  4. **بخش جدید «صفحه اصلی» در پنل ادمین:** صفحهٔ placeholder `Areas/Admin/Pages/Home/Index.cshtml(.cs)` (route `/Admin/Home`، تحت پوشش policy `AdminOnly` به‌صورت خودکار)، لینک در `_AdminSidebar` بعد از داشبورد، کلاس CSS `admin-empty-state` در `admin.css`. آمادهٔ افزودن کنترل‌های صفحهٔ اصلی در آینده.
  - **توجه:** بیلد/تست در این محیط (Linux sandbox، بدون dotnet) اجرا نشد؛ تغییرات صرفاً View/JS/CSS + یک Razor Page ساده هستند و طبق conventions موجود نوشته شدند.
- 2026-06-13: **اصلاحات موبایل هدر + بررسی بخش گرید دستهٔ پنل (به‌خواست کاربر):**
  1. **رفع پرشِ دراور همبرگری در RTL (`wwwroot/css/site.css`):** پنل دراور از `inset-inline-end:0` + `translateX(100%)` بود؛ چون `inline-end` در RTL = «چپ» و خودِ `translateX`/transform به `dir` وابسته نیست، پنل از راست می‌آمد ولی روی **چپ** می‌نشست. اصلاح: مقادیر **فیزیکی** `right:0; left:auto` + `translate3d(100%,0,0)` (خارج، سمت راست) → باز: `translate3d(0,0,0)`. حالا دقیقاً از راست می‌آید و روی **راست** می‌ماند. سایه هم `-18px 0` (به چپ) درست بود.
  2. **خروج از حساب در هدرِ موبایل = از قبل درست بود:** فرم Logout در `_Header.cshtml` کلاس `d-none d-lg-block` دارد (فقط دسکتاپ)؛ در موبایل خروج فقط از داخل دراور انجام می‌شود. تغییری لازم نبود.
  3. **بخش «گرید دسته‌بندی صفحهٔ اصلی» در پنل ادمین = از قبل کامل پیاده‌سازی شده بود** (کامیت‌های `MAIN PAGE`/`header and fother`): انتخاب دسته‌ها (افزودن/حذف)، مرتب‌سازی هوشمند (درگ‌ودراپ + دکمه‌های بالا/پایین)، ۴ سبک ظاهری با کارت پیش‌نمایش (Bento/Grid/Scroll/Pills)، عنوان/ترتیب/فعال‌بودن — همگی end-to-end از پنل تا `Views/Home/_CategoryGrid.cshtml`. **بهبود این سشن:** افزودن **پشتیبانی لمسی (touch drag)** به `wwwroot/admin/home-categories.js` تا مرتب‌سازی درگ‌ودراپ روی موبایل/تبلت هم کار کند (گرفتن دستگیره → جابه‌جایی با لمس).
  - **توجه:** بیلد در این محیط اجرا نشد (بدون dotnet)؛ تغییرات فقط CSS/JS و با `node --check` از نظر سینتکس تأیید شد.
  - **اسکیل‌ها:** کاربر خواست از `caveman`/`frontend-design`/`ux-designer` استفاده شود؛ این اسکیل‌ها در محیط فعلی **در دسترس نبودند** (`/mnt/skills/` موجود نیست و activate خطای not available داد). کار با اصول طراحی/UX به‌صورت دستی انجام شد.
- 2026-06-13: **بازرسی کامل کدبیس + بهینه‌سازی مستندات برای AI (به‌خواست کاربر):**
  - **بررسی جزء‌به‌جز** کل ساختار (5 پروژه، 21 entity، 6 enum، 100 فایل Feature شامل 61 ادمین، 7 migration، 6 controller، 27 view، 32 صفحهٔ Razor ادمین، سرویس‌ها، Program.cs، DbContext، الگوی CQRS) انجام شد.
  - **فایل جدید `docs/AI_CONTEXT.md`** (سبک caveman — فشرده/پرسیگنال): نقشهٔ کامل پروژه برای AI تا با حداقل توکن جهت‌یابی کند بدون اسکن کل کدبیس. شامل: نگاه کلی، وضعیت، نقشهٔ ۵ پروژه + جهت وابستگی، الگوی CQRS کپی‌پیست، «کجا چی پیدا کنم» (همهٔ مسیرهای پرکاربرد)، قانون‌های اجرایی، چرخهٔ کار، و gotchaها.
  - **اصلاح ناهماهنگی‌های مستندات** که در بازرسی کشف شد:
    1. `CLAUDE.md` به سه فایل **ناموجود** ارجاع می‌داد (`ROADMAP.md`، `ADMIN_ROADMAP.md`، `ADMIN_PROGRESS.md`) → ارجاعات حذف/اصلاح، بخش Pointers فقط فایل‌های موجود + ارجاع به `AI_CONTEXT.md` به‌عنوان «اول این را بخوان».
    2. `PROGRESS.md` می‌گفت «Current phase = Phase 7» و «P9 Admin کد=صفر» در حالی که پنل ادمین **عملاً کامل پیاده‌سازی شده و زنده است** (Areas/Admin + Features/Admin + بلوک‌های Program.cs) → Current phase به «Phase 9 در حال توسعه» و وضعیت P9 به `[~]` اصلاح شد.
  - **توجه:** بیلد در این محیط (Linux sandbox، بدون dotnet) اجرا نشد؛ تغییرات فقط مستندات (`.md`) هستند، صفر تغییر کد.
