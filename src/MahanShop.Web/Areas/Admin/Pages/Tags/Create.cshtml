@page
@model MahanShop.Web.Areas.Admin.Pages.Tags.CreateModel
@{
    ViewData["Title"] = "برچسب جدید";
    ViewData["AdminActive"] = "tags";
}

<div class="admin-panel-box admin-form">
    <div asp-validation-summary="All" class="text-danger"></div>
    <form method="post">
        <div class="mb-3">
            <label class="form-label">نام برچسب</label>
            <input asp-for="Name" class="form-control" />
        </div>
        <div class="mb-3">
            <label class="form-label">نامک (اختیاری — از نام ساخته می‌شود)</label>
            <input asp-for="Slug" class="form-control" />
        </div>
        <div class="admin-form__actions">
            <button type="submit" class="btn btn-primary">ذخیره</button>
            <a asp-page="Index" class="btn btn-outline-secondary">انصراف</a>
        </div>
    </form>
</div>
