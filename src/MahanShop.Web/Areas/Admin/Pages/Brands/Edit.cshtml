@page "{id:int}"
@model MahanShop.Web.Areas.Admin.Pages.Brands.EditModel
@{
    ViewData["Title"] = "ویرایش برند";
    ViewData["AdminActive"] = "attributes";
}

<div class="admin-panel-box admin-form">
    <div asp-validation-summary="All" class="text-danger"></div>
    <form method="post" enctype="multipart/form-data">
        <input type="hidden" asp-for="Id" />
        <div class="mb-3">
            <label class="form-label">نام برند</label>
            <input asp-for="Name" class="form-control" />
        </div>
        <div class="mb-3">
            <label class="form-label">نامک</label>
            <input asp-for="Slug" class="form-control" />
        </div>
        <div class="mb-3">
            <label class="form-label">لوگو</label>
            @if (!string.IsNullOrEmpty(Model.LogoUrl))
            {
                <div><img src="@Model.LogoUrl" alt="" class="admin-thumb admin-thumb--lg" /></div>
            }
            <input asp-for="LogoFile" type="file" accept="image/*" class="form-control mt-2" />
            <input type="hidden" asp-for="LogoUrl" />
        </div>
        <div class="mb-3">
            <label class="form-label">ترتیب نمایش</label>
            <input asp-for="DisplayOrder" type="number" class="form-control" />
        </div>
        <div class="form-check mb-3">
            <input asp-for="IsActive" class="form-check-input" />
            <label class="form-check-label">فعال</label>
        </div>
        <div class="admin-form__actions">
            <button type="submit" class="btn btn-primary">ذخیره</button>
            <a asp-page="Index" class="btn btn-outline-secondary">انصراف</a>
        </div>
    </form>
</div>
